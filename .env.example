# =============================================================================
# CONFIGURAÇÃO DE AMBIENTE - Livraria Online
# =============================================================================
# Este arquivo serve como template. Copie para .env e preencha os valores reais.
# IMPORTANTE: Nunca commitar o arquivo .env real com credenciais!
# =============================================================================

# Ambiente de execução (development ou production)
DJANGO_ENV=development

# Chave secreta do Django (gerar nova em produção)
# Gerar com: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-gere-uma-chave-secreta-para-producao

# Domínios permitidos (separados por vírgula, sem espaços)
ALLOWED_HOSTS=localhost,127.0.0.1,seu-dominio.com,www.seu-dominio.com

# Domínio principal do site (para SEO e emails)
SITE_DOMAIN=seu-dominio.com

# =============================================================================
# CONFIGURAÇÃO DE EMAIL (SMTP)
# =============================================================================

# Para Gmail:
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=seu-email@gmail.com
EMAIL_HOST_PASSWORD=sua-senha-de-aplicacao-do-gmail

# Para SendGrid:
# EMAIL_HOST=smtp.sendgrid.net
# EMAIL_PORT=587
# EMAIL_HOST_USER=apikey
# EMAIL_HOST_PASSWORD=sua-api-key-do-sendgrid

# =============================================================================
# CONFIGURAÇÃO DE BASE DE DADOS (POSTGRESQL)
# =============================================================================
# Para desenvolvimento, pode usar SQLite (padrão)
# Para produção, recomenda-se PostgreSQL

DB_ENGINE=django.db.backends.postgresql
DB_NAME=livraria_db
DB_USER=livraria_user
DB_PASSWORD=senha-segura-aqui
DB_HOST=localhost
DB_PORT=5432

# Alternativamente, pode usar uma URL de conexão completa:
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# =============================================================================
# CONFIGURAÇÃO DE PAGAMENTOS (OPCIONAL)
# =============================================================================

# Stripe (para pagamentos com cartão)
STRIPE_PUBLIC_KEY=pk_test_sua-chave-publica
STRIPE_SECRET_KEY=sk_test_sua-chave-secreta
STRIPE_WEBHOOK_SECRET=whsec_sua-chave-webhook

# MB Way API (se disponível)
MBWAY_API_KEY=sua-chave-api-mbway
MBWAY_API_SECRET=seu-segredo-api-mbway

# =============================================================================
# SERVIÇOS DE TERCEIROS (OPCIONAL)
# =============================================================================

# Google Analytics
GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# Google Search Console
GOOGLE_SITE_VERIFICATION=sua-chave-de-verificacao

# Facebook Pixel
FACEBOOK_PIXEL_ID=123456789012345

# =============================================================================
# CONFIGURAÇÕES DE SEGURANÇA
# =============================================================================

# Ativar HTTPS (true em produção)
SECURE_SSL_REDIRECT=false

# Tempo de sessão em segundos (padrão: 1209600 = 2 semanas)
SESSION_COOKIE_AGE=1209600

# =============================================================================
# LOGS E MONITORIZAÇÃO
# =============================================================================

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Sentry DSN (para rastreamento de erros)
SENTRY_DSN=https://sua-chave@sentry.io/seu-projeto

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================
#
# 1. SEGURANÇA:
#    - Nunca partilhe este arquivo com credenciais reais
#    - Use senhas fortes e únicas
#    - Ative autenticação de dois fatores onde possível
#
# 2. GMAIL:
#    - Ative "Verificação em 2 etapas" na conta
#    - Crie uma "Senha de aplicação" específica
#    - https://myaccount.google.com/apppasswords
#
# 3. POSTGRESQL:
#    - Instale: pip install psycopg2-binary
#    - Configure o user e database no PostgreSQL primeiro
#
# 4. PRODUÇÃO:
#    - Sempre use DEBUG=False
#    - Configure ALLOWED_HOSTS corretamente
#    - Use HTTPS (SSL/TLS)
#    - Faça backups regulares da base de dados
#
# 5. PARA CARREGAR AS VARIÁVEIS:
#    - Instale python-decouple: pip install python-decouple
#    - Ou use django-environ: pip install django-environ
#
# =============================================================================
